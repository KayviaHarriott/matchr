import React, { FC } from "react";

interface FullPageScrollProps{
    sections: React.ReactNode[];
}
export const FullPageScroll: FC<FullPageScrollProps> = ({sections}) => {

    return (
        <>
            <p>Scroll</p>
        </>
       
    )
}