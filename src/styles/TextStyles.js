export const textHeaderStyle = (darkMode) => `${darkMode ? `text-[white]` : ``}`;
export const textBodyStyle = (darkMode) => `${darkMode ? `text-[white]` : `text-[darkgray]`}`;
export const TableHeaderStyle = { fontWeight: "bold", color: "#5D5D5D" };
