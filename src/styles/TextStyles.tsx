export const textHeaderStyle = (darkMode: boolean) =>
  `${darkMode ? `text-[white]` : ``}`;

export const textBodyStyle = (darkMode: boolean) =>
  `${darkMode ? `text-[white]` : `text-[darkgray]`}`;

  export const TableHeaderStyle = {fontWeight: "bold", color: "#5D5D5D"}